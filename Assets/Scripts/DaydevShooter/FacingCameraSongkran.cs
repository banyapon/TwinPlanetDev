using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FacingCameraSongkran : MonoBehaviour
{
    
  /*  Camera cameraPlayer;
    // Start is called before the first frame update
    void Start()
    {
        GameObject newCamera = new GameObject();

        if(cameraPlayer == null){
            newCamera = GameObject.FindGameObjectWithTag("CameraPlayer");
            cameraPlayer = newCamera.GetComponent<Camera>();
        }
    }

    // Update is called once per frame
    void Update()
    {
        if(cameraPlayer != null){
            transform.LookAt(cameraPlayer.transform.position, Vector3.up);
        }
    }*/
}
